server:
  port: 8082

spring:
  application:
    name: response-service

  datasource:
    url: jdbc:postgresql://localhost:5432/campaign_db
    username: postgres
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
