server:
  port: 8084

spring:
  application:
    name: reporting-service

  datasource:
    url: jdbc:postgresql://localhost:5432/campaign_db
    username: postgres
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
